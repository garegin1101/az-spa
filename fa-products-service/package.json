{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "// Please use belowmentioned commands this way": "export [variable name]=[value name] &&  [command]",
    "azure:publish": "func azure functionapp publish %FUNCTION_APP_NAME%",
    "azure:add-slot": "az functionapp deployment slot create  --name %FUNCTION_APP_NAME% --resource-group %RESOURCE_GROUP_NAME% --slot %SLOT_NAME%",
    "azure:auto-swap": "az functionapp deployment slot auto-swap  --name %FUNCTION_APP_NAME% --resource-group %RESOURCE_GROUP_NAME% --slot %SLOT_NAME%"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "18.x",
    "@types/uuid": "^9.0.7",
    "azure-functions-core-tools": "^4.x",
    "typescript": "^4.0.0"
  }
}
